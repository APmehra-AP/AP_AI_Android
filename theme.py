# Created by : Amarchand Meghwal

from kivy.metrics import dp


class Theme:

    # -----------------------
    # APP
    # -----------------------

    APP_NAME = "AP AI"
    VERSION = "V2.0 Final"

    # -----------------------
    # COLORS
    # -----------------------

    BG = (0.03, 0.04, 0.08, 1)

    SURFACE = (0.08, 0.10, 0.16, 1)

    CARD = (0.12, 0.15, 0.23, 1)

    PRIMARY = (0.00, 0.75, 1.00, 1)

    ACCENT = (0.10, 0.90, 1.00, 1)

    SUCCESS = (0.18, 0.85, 0.42, 1)

    WARNING = (1.00, 0.75, 0.12, 1)

    ERROR = (1.00, 0.28, 0.28, 1)

    TEXT = (1, 1, 1, 1)

    TEXT_SECONDARY = (.72, .78, .88, 1)

    # -----------------------
    # UI
    # -----------------------

    RADIUS = dp(22)

    PADDING = dp(20)

    GAP = dp(16)

    ICON = dp(30)

    HEADER = dp(36)

    BUTTON = dp(52)

    CARD_HEIGHT = dp(115)
